# Required on Vercel for a shared database (Turso integration sets these).
# Do not commit real values. Local development uses file SQLite by default.
TURSO_DATABASE_URL=libsql://your-db.turso.io
TURSO_AUTH_TOKEN=

# Optional AI provider for the Playbook Brief. The app is fully demoable without it.
# OPENAI_API_KEY=
